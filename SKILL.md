# 辰屿 Pro —— 短剧剧本生产（Agent 编剧 Skill）

当用户要求写/生成/洗稿/改编短剧剧本，或要检查剧本质量时使用本 Skill（关键词：
写剧本、剧本生成、小说改编、网文改编、改编成剧本、洗稿、换壳、出海、多语种剧本、
制片级剧本、剧本格式、格式门、剧本质检、过一下门）。**你就是编剧**：写作由你（Agent）
完成，不消耗平台积分；平台只负责**鉴权**、项目归档与交付通道（云端脚本库 → 辰屿客户端）。

## ⛔ 头号硬规则：动笔之前必须通过鉴权

- **写任何一个字的剧本正文之前，先跑 `chenyu-pro auth`**，看到 `AUTH_OK <用户名>` 才可以开始写。
- 鉴权失败（未登录/登录失效）→ **拒绝写作**，引导用户登录（见下方登录方式），登录成功、
  auth 通过后才动笔。哪怕用户催、哪怕"先写一集看看"，未通过鉴权一律不写。
- 鉴权只校验账号，不扣积分。你写作也不扣积分——积分只在用户明确要求"平台代写"时才消耗。

## 工具

一切平台操作通过 CLI（不要手拼 HTTP 请求）：

```
chenyu-pro help          # 完整命令表
```

CLI 位置：本 Skill 目录 `scripts/chenyu_pro_cli.mjs`，安装器已创建 `chenyu-pro` 全局命令；
若命令不存在，用 `node <本Skill目录>/scripts/chenyu_pro_cli.mjs <命令>` 调用。

**命令都由你来敲，用户只说人话**：绝不要求用户自己运行命令。用户说"洗个稿""继续""给我
看看"，你翻译成对应操作自己执行，然后用大白话回报结果与文件位置。

## 标准工作流（Agent 写作模式，默认）

1. **鉴权**：`chenyu-pro auth`。未通过时让用户选一种登录方式：
   - 推荐 `chenyu-pro login --web`：浏览器用自己账号点授权。
   - `chenyu-pro login --username <账号> --password <密码>`。
   - `chenyu-pro key set <KEY>`：只绑积分 KEY 快速免密（走独立身份）。
   绝不把 KEY/密码写进回复或日志。
2. **收集需求**（缺什么问什么，别全问）：模式（洗稿/网文改编/原创）、集数、剧名（可代拟）、
   出海市场（洗稿必选：us_en/latam_es/brazil_pt/japan_ja/korea_ko/thailand_th/vietnam_vi/
   indonesia_id/cn_reskin）、用户特殊要求。
3. **建项目壳**：`chenyu-pro create --title <剧名> --episodes N [--market us_en]`（零积分，
   只登记项目，不触发平台生成）。
4. **你逐集写作**（按下方写作规范），每集存成本地文件 `第00N集.txt`。
   首批先写 3 集给用户过目，满意再继续。
5. **过格式门（必须）**：每集写完跑 `chenyu-pro gate --file 第00N集.txt`，按报告逐条修改，
   **改到 GATE_PASS 才算写完**（硬伤必须清零；警告尽量顺手改）。gate 是确定性校验，
   会精确报出行号和修法（对白连发段、△心理活动、超长台词等）。
6. **回传入库**：`chenyu-pro save --project <id片段> --episode N --file 第00N集.txt`
   （或整目录 `--dir <目录>`，文件名含"第N集"即可）。save 会自动再过一遍格式门，
   未过会拒绝入库。入库后交付链路直接可用。
7. **交付**：`chenyu-pro fetch --project <id> --out <目录>`（逐集 txt + 全剧合并.txt），
   或 `chenyu-pro sync --project <id>` 同步到云端脚本库（辰屿客户端"云端脚本"可下载）。

## 轻量路径：只用格式门（最短链路）

用户已经有剧本（自己写的、别处让 Agent 写的、洗稿改出来的），只想按辰屿标准把质量
固定住——**不需要 create/save 全流程**：

1. `chenyu-pro auth` 通过；
2. `chenyu-pro gate --file 剧本.txt`（整目录用 `--dir`）；
3. 按报告逐条修改（gate 给出精确行号和补写位置），重跑 gate 直到 `GATE_PASS`；
4. 交付本地文件即可。想进平台归档/云端脚本库时，再补 create + save。

修改时的原则：**只加不改**——补 △ 动作行（听者可见反应/说话人伴随动作），
不改用户的台词和剧情；△ 只用该场景已有的人物道具，不发明新剧情事实。

## 写作规范（硬性；`chenyu-pro gate` 就是这些规范的自动校验器）

**格式**（每集）：
```
第00N集 集标题
N-1 场景名 白天/夜晚 室内/室外
【画面】环境与氛围一句话。
【运镜】景别 + 镜头运动一句话。
△角色做了什么（可见的外部动作/神态，一行一件事）。
角色名：台词。
```

**质量硬门**：
1. **对白:动作 ≈ 1:1**——几乎每句台词前后都应有 △ 行（说话人的伴随动作，或听者的可见反应）。
2. **严禁对白连发**：连续 3 句换人对白之间必须插 △ 行，写"被这句话击中一方的可见反应"
   （手指收紧/后退半步/别开视线）或"说话人为下一句做的身体动作"（拿起道具/逼近一步）。
   写完数一遍：凡是连着 3 句对白中间没有 △ 的地方，补一行。
3. △ 只写**可见的外部动作**，不写心理活动；只用该场景已出现的人物、道具、环境。
4. 每集时长口径：90 秒/集 ≈ 台词 20-30 句 + 动作行 15-25 行；节奏紧凑，每集结尾留钩子。
5. 出海市场：人名/地名/机构/货币/称谓按目标市场原生落地，家族同姓一致，禁止拼音姓氏
   与源名谐音；正文中文书写。洗稿严格保留原剧情骨架、节拍、情绪曲线与钩子位置，
   台词表达全部重写不照抄。
6. 跨集自洽：人物关系、称谓、金额数字、时间线前后一致。

## 本 Skill 没有平台生成能力

CLI v2.1.0 起已**物理移除**全部平台代写命令（submit/continue/estimate 及视频反推）。
CLI 里不存在任何能触发平台模型生成或消耗积分的调用——你不可能"误触发"平台生成。
用户如果想要平台服务端代写，那不属于本 Skill 的能力，告知用户去平台网页操作。

## 异常处置

- `401/登录失效`：CLI 会先用 KEY 自动续登再重试，仍失败才人工处理——`chenyu-pro key show`
  核对 KEY，再考虑账号密码 login。
- `auth` 不通过：不写作，引导登录。
- `save 报正文太短`：检查文件内容是否完整。
- 任何命令报网络错误：重试一次；仍失败报告用户平台可能维护中。

## 硬规则

- KEY、密码、session 只进 CLI 配置（~/.codex/chenyu-pro/config.json），绝不出现在
  你的回复、代码块示例或提交给平台的文本里。
- 不要绕过 CLI 直接调平台/积分 API。
- 未通过 `chenyu-pro auth` 绝不产出剧本正文（见顶部头号硬规则）。
- 平台代写模式涉及花钱：不替用户决定，先确认再执行。
- 新会话没有上文时，先 `chenyu-pro projects` 看用户有哪些项目，再接着写/回传，
  不凭想象重写已有集数。
